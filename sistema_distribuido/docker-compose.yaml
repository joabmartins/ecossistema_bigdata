version: '3.8'

services:
  master:
    image: alpine:latest
    container_name: master-node
    hostname: master-node
    command: sh -c "echo 'Master node executando.' && slep infinity"

  worker1:
    image: alpine:latest
    container_name: worker1-node
    hostname: worker1-node
    command: sh -c "echo 'Worker node 1 executando.' && slep infinity"

  worker2:
    image: alpine:latest
    container_name: worker2-node
    hostname: worker2-node
    command: sh -c "echo 'Worker node 2 executando.' && slep infinity"