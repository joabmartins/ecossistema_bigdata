services: 
  master:
    image: alpine:latest
    container_name: master-node
    hostname: master-node
    command: sh -c "echo 'Master node executado.' && sleep infinity"

  worker1:
    image: alpine:latest
    container_name: worker1-node
    hostname: worker1-node
    command: sh -c "echo 'Worker node 1 executado.' && sleep infinity"

  worker2:
    image: alpine:latest
    container_name: worker2-node
    hostname: worker2-node
    command: sh -c "echo 'Worker node 2 executado.' && sleep infinity"